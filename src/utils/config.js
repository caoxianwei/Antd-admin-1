const APIROOT = '/api';

module.exports = {
    apiPrefix:'/api',
    api:{
        userLogin: `${APIROOT}/user/login`,
        userLogout: `${APIROOT}/user/logout`,
        users: `${APIROOT}/users`
    }
}
