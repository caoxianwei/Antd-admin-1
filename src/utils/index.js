/**
 *  @desc 工具函数
 *  global window
 */
import classnames from 'classnames' //joining classNames together
import config from './config'
import request from './request'
import { color } from './theme'

export {
    config,
    request,
    color,
    classnames
}
